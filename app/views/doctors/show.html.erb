<h1><%= @doctor.name %></h1>
<ul>
<% @doctor.appointments.each do |app| %>
    <li>Date: <%= app.appointment_datetime.strftime("%B %d, %Y at %k:%M") %><br>
    Name: <%= link_to app.patient.name, patient_path(app.patient.id) %></li>
<% end %>
</ul>
